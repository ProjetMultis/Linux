cd /(projet)
echo "# Linux" >> README.md

initialisation du .git
git init

ajout du Readme
git add README.md

commit 
git commit -m "nom commit"

ajout au résau sociaux github
git remote add origin https://github.com/tonProjet/linux.git

intialise sur ton git
git push -u origin master

doc:
https://wiki.archlinux.org/index.php/git

si ajouter un dossier il faut
aller dans le dossier avec un cd
puis un git add nom_du_fichier
ensuite 
- git status pour voir les commits
- git commit -m "nom commit"
- git push -u origin master et taper le user et le mdp

pour mettre à jour de github sur le navigateur en local faire un
git pull


